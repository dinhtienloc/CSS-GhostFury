/* Include File generated by Pawn Studio */

#include <sourcemod>

#if defined _ghostfury_included_
  #endinput
#endif
#define _ghostfury_included_

enum PlayerClass {
	TEAM_HUMAN = 0,
	TEAM_GHOST
}

/**
 * Returns true if the player is a hidden, false if not. 
 *
 * @param iClient			The client index.
 *  
 * @return			        True or false.
 **/
native bool GF_IsPlayerGhost(int clientId);

/**
 * Returns true if the player is a human, false if not.
 *
 * @param iClient			The client index.
 *  
 * @return			        True or false.
 **/
native bool GF_IsPlayerHuman(int clientId);

/**
 * Force to set a player's class.
 *
 * @param class			The class type. See enum PlayerClass.
 * @param iClient		The client index.
 *  
 * @noreturn
 **/
native void GF_SetPlayerClass(int clientId, int class);

